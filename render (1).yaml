services:
  - type: web
    name: cybesure-backend
    env: docker
    plan: starter
    autoDeploy: true
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: OPENAI_API_KEY
        sync: false
